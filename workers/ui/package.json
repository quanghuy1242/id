{
  "name": "@idco/ui-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vinext dev --cwd .",
    "typecheck": "tsgo -p tsconfig.json --noEmit",
    "test": "vitest run --config vitest.config.ts"
  }
}
