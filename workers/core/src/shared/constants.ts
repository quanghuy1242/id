/** HTTP status returned when a request succeeds. */
export const HTTP_OK = 200;

/** HTTP status returned after creating a resource. */
export const HTTP_CREATED = 201;

/** Cache key used for enabled resource-server audiences in Phase 2. */
export const RESOURCE_SERVER_AUDIENCES_CACHE_KEY = "id-resource-servers:audiences";
