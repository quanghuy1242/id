/**
 * Operations placeholder for the OAuth client picker plugin. The verification logic
 * lives inline in `index.ts` as a single self-contained function. Required by the
 * `architecture/auth-plugin-folder-shape` linter.
 */
export const placeholder = true;
