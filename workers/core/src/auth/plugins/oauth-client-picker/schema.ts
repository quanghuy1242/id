/**
 * Schema placeholder for the OAuth client picker plugin. The picker is a read-only
 * wrapper around `oauthClient` rows; it owns no schema of its own. Required by the
 * `architecture/auth-plugin-folder-shape` linter.
 */
export const placeholder = true;
